import { BloqueConsumoPipe } from './bloque-consumo.pipe';

describe('BloqueConsumoPipe', () => {
  it('create an instance', () => {
    const pipe = new BloqueConsumoPipe();
    expect(pipe).toBeTruthy();
  });
});
