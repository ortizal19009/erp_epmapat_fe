import { EstadoEmisionPipe } from './estado-emision.pipe';

describe('EstadoEmisionPipe', () => {
  it('create an instance', () => {
    const pipe = new EstadoEmisionPipe();
    expect(pipe).toBeTruthy();
  });
});
