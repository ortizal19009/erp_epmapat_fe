import { NombreEmisionPipe } from './nombre-emision.pipe';

describe('NombreEmisionPipe', () => {
  it('create an instance', () => {
    const pipe = new NombreEmisionPipe();
    expect(pipe).toBeTruthy();
  });
});
