import { FormatFechaPipe } from './format-fecha.pipe';

describe('FormatFechaPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatFechaPipe();
    expect(pipe).toBeTruthy();
  });
});
